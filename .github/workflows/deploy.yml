name: Build and Deploy to K8s

on:
  push:
    branches:
      - master

env:
  REGISTRY: ghcr.io
  OWNER: khalilksa33

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.OWNER }}/26i-consult-backend:latest
            ${{ env.REGISTRY }}/${{ env.OWNER }}/26i-consult-backend:${{ github.sha }}

      - name: Build and push Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.OWNER }}/26i-consult-frontend:latest
            ${{ env.REGISTRY }}/${{ env.OWNER }}/26i-consult-frontend:${{ github.sha }}

  deploy-to-k8s:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install cloudflared
        run: |
          curl -L --output cloudflared.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared.deb

      - name: Update deployment with new image tags
        run: |
          sed -i 's|ghcr.io/khalilksa33/26i-consult-backend:.*|ghcr.io/khalilksa33/26i-consult-backend:${{ github.sha }}|g' k8s/deployment.yaml
          sed -i 's|ghcr.io/khalilksa33/26i-consult-frontend:.*|ghcr.io/khalilksa33/26i-consult-frontend:${{ github.sha }}|g' k8s/deployment.yaml

      - name: Configure SSH and Deploy
        env:
          SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SSH_USER: ${{ secrets.SSH_USERNAME }}
          SSH_HOST_NAME: ${{ secrets.SSH_HOST_NAME }}
          TUNNEL_SERVICE_TOKEN_ID: ${{ secrets.CF_CLIENT_ID }}
          TUNNEL_SERVICE_TOKEN_SECRET: ${{ secrets.CF_CLIENT_SECRET }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          
          # Setup SSH to use Cloudflare proxy
          cat << EOF > ~/.ssh/config
          Host $SSH_HOST_NAME
            ProxyCommand cloudflared access ssh --hostname %h
            StrictHostKeyChecking no
            User $SSH_USER
          EOF
          
          # Stream the updated yaml file to kubectl on Host B over SSH
          cat k8s/deployment.yaml | ssh $SSH_HOST_NAME "sudo k3s kubectl apply -f -"
          
          # Verify rollouts
          ssh $SSH_HOST_NAME "sudo k3s kubectl rollout status deployment/26i-consult-backend"
          ssh $SSH_HOST_NAME "sudo k3s kubectl rollout status deployment/26i-consult-frontend"
