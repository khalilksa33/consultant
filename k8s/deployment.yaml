apiVersion: apps/v1
kind: Deployment
metadata:
  name: al-diwan-backend
  labels:
    app: al-diwan-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: al-diwan-backend
  template:
    metadata:
      labels:
        app: al-diwan-backend
    spec:
      containers:
      - name: backend
        image: ghcr.io/khalilksa33/al-diwan-backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        env:
        - name: MONGODB_URI
          value: "mongodb://mongodb:27017/al_diwan"
        - name: JWT_SECRET
          value: "super_secret_jwt_key_26i_consulting_app"
---
apiVersion: v1
kind: Service
metadata:
  name: al-diwan-backend-service
spec:
  selector:
    app: al-diwan-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: al-diwan-frontend
  labels:
    app: al-diwan-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: al-diwan-frontend
  template:
    metadata:
      labels:
        app: al-diwan-frontend
    spec:
      containers:
      - name: frontend
        image: ghcr.io/khalilksa33/al-diwan-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        env:
        - name: NEXT_PUBLIC_API_URL
          value: "http://al-diwan-backend-service:5000"
---
apiVersion: v1
kind: Service
metadata:
  name: al-diwan-frontend-service
spec:
  type: LoadBalancer
  selector:
    app: al-diwan-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000